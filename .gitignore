# Android 构建产物
app/build/
build/
.gradle/
local.properties
*.apk
*.aab

# 签名密钥与凭据（绝不入库）
*.keystore
*.jks
*.pepk
*.p12
*.pfx
*.pem
*.key
keystore.properties
signing.properties
.env
secrets*.json
credentials*.json

# IDE
.idea/
*.iml
.vscode/

# Python
__pycache__/

# 抓帧与临时产物（含游戏截图，不入库）
_tools/*.jpg
_tools/*.png
_tools/neg_frames/
_tools/*.log

# 流程编辑器/模板框选工具已独立到 E:\MaaWH Stdio（见 AGENTS.md 第〇节）；
# 这条只用于忽略仓库里可能残留的旧目录，不再跟踪其中任何文件
FlowEditor/

# 根目录调试 dump 与临时产物
/*.xml
/*.txt
/*.json
/*.png
/*.jpg
/*.xlsx

# 大体积非项目源码
shots_login/
platform-tools/
_dl/
refs/
mfw-android/

# 历史备份目录（含重复大文件）
_tools/appsrc_backup_*/
_tools/whmx_backup_*/

# 构建时自动生成的 assets 副本（由 whmx/ 同步而来）
app/src/main/assets/whmx/

# 预编译库来源目录（内嵌 git 仓库；so 已在 app/src/main/jniLibs）
MaaFramework/

# 迁移/修复工具写的本地备份（git 里有历史，不必入库）
whmx/**/*.bak
whmx/**/*.bak_*
